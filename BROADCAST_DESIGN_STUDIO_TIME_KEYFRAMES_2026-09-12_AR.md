# WAB-TKD — Broadcast Design Studio — Time-Local Editing

تم توسيع Broadcast Design Studio بحيث تكون تعديلات التصميم المرتبطة بالـTimeline محلية للحظة الزمنية المحددة بدل تغيير الشكل الأساسي للـAnimation.

## السلوك الجديد

- عند التعديل في `0s` يتم تعديل الوضع الأساسي، وإذا كان هناك Keyframe في `0s` يتم تحديثه.
- عند التعديل في `>0s` يتم إنشاء/تحديث Keyframe تلقائيًا في تلك اللحظة.
- تعديل X/Y/Width/Height/Scale/Rotation/Opacity والألوان والإطارات والخطوط والنصوص والصور والـEffects وغيرها من خصائص التصميم يمكن حفظه كلحظة زمنية.
- عند الرجوع إلى لحظة قبل التعديل لا يظهر التعديل اللاحق.
- عند تجاوز لحظة التعديل يبقى التعديل فعالًا حتى Keyframe لاحق، مع interpolation للخصائص الرقمية.
- `FREEZE` يعرض الحالة الدقيقة للـTimeline ويمكن تعديلها ثم متابعة التشغيل.
- الشاشة الثانية تستخدم نفس Design Data وتقوم بتقييم Keyframes عند وقت التشغيل بدل استعمال قيمة ثابتة.
- أثناء Animation حقيقية يتم اشتقاق وقت التصميم من `animationController.startedAt` عندما يكون متاحًا.
- التعديل لا يغير Original Animation Base؛ بل يُحفظ داخل Draft/Design data.

## التوافق

تم الحفاظ على حقول Keyframe القديمة (`x/y/scale/rotation/opacity/width/height`) حتى لا تنكسر التصميمات السابقة، مع إضافة `props` العامة للخصائص القابلة للتحريك.

## التحقق

تمت مراجعة الملفات المعدلة نصيًا. تعذر إجراء TypeScript/Vite build كامل في بيئة العمل لأن المشروع المرفق لا يحتوي `node_modules` والاعتماديات غير متاحة محليًا؛ لذلك لا يتم الادعاء بأن Production Build تم اجتيازه هنا.
